{
  "name": "education-platform",
  "version": "1.0.0",
  "description": "منصة التعليم الجزائري",
  "main": "index.js",
  "scripts": {
    "start": "netlify dev",
    "build": "echo 'No build required for static site' && exit 0",
    "dev": "netlify dev",
    "test": "echo 'No tests configured'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "mongodb": "^4.5.0",
    "jsonwebtoken": "^8.5.1",
    "node-fetch": "^2.6.7",
    "netlify-cli": "^12.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
